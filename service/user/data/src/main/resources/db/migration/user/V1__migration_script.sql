CREATE TABLE IF NOT EXISTS PROFILE (ID BIGINT AUTO_INCREMENT PRIMARY KEY, "name" VARCHAR(255) NOT NULL, LAST_NAME VARCHAR(255) NOT NULL, EMAIL VARCHAR(320) NOT NULL, PHONE VARCHAR(255) NULL, CREATED_AT DATETIME(9) NOT NULL, UPDATED_AT DATETIME(9) NOT NULL);
