<configuration>

    <!-- Rolling and compressing are done on the Dockers side-->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{ISO8601} [%level] [${BOOKK_ME_SERVICE_NAME}:${BOOKK_ME_SERVICE_VERSION}] [%logger{35}]: %msg%n
            </pattern>
        </encoder>
    </appender>

    <!-- Root -->
    <root level="info">
        <appender-ref ref="STDOUT"/>
    </root>
    <root level="debug">
        <appender-ref ref="STDOUT"/>
    </root>

    <!-- Logger levels -->
    <logger name="com.zaxxer.hikari" level="info"/>
    <logger name="org.apache.kafka" level="info"/>
    <logger name="io.netty" level="info"/>
    <logger name="com.wolt" level="error"/>
</configuration>